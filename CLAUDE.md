# CLAUDE.md

给 Claude Code 在本仓库工作时的说明。

## 项目是什么

hanyu 的 AI 笔记站：用 [MkDocs](https://www.mkdocs.org/) + Material 主题，把 Obsidian 里的笔记发布成静态网站。
站点标题「hanyu的笔记」，主题「关于AI的一些有趣的事、人和思考」，界面语言为中文。

仓库里目前只有 `mkdocs.yml` —— **笔记正文不在这个仓库里**。

## 关键约定

- **`docs_dir` 指向本地 Obsidian 库**：`/Users/jennie/Documents/Obsidian Vault/小红书图文`。
  - 这是 jennie 本机的绝对路径，只在她的 Mac 上能构建成功；在 CI、云端会话或别人的机器上 `mkdocs build` 会因为找不到目录而失败，这是预期行为，不要"顺手修复"成相对路径。
  - 因此在云端 / 远程会话里**无法读取或校验笔记内容**，只能改配置。
- **笔记按日期目录组织**：`2026.3.5/The Skip 播客笔记.md` 这种 `年.月.日/标题.md` 结构；同一天多篇就放在同一个日期目录下。跨多天的系列用区间目录，如 `2026.3.17-3.24/`。
- **文件名和导航标题都用中文**，路径里带空格是正常的，写 `nav` 时不用转义、也不用加引号（YAML 里冒号后跟空格即可）。

## 常见任务

### 新增一篇笔记

正文写在 Obsidian 库里，然后只需在 `mkdocs.yml` 的 `nav:` 下追加一条：

```yaml
    - 简短标题: 2026.3.20/文章文件名.md
```

规则：
- 加在 `hanyu的笔记` 这一节的**末尾**，保持按日期从早到晚排列。
- `nav` 里的标题写简短的主题词（如 `MiniMax财报`），不必和文件名一致。
- 同一主题有多篇（如播客记录 + 小红书图文），用嵌套子节点分组：

```yaml
    - 谢赛宁访谈:
      - 思考笔记: 2026.3.17-3.24/播客记录+思考笔记_part 1.md
      - 完整访谈: 2026.3.17-3.24/谢赛宁 X 张晓珺 transcript 完整版.md
```

### 本地预览 / 构建

仅在 jennie 的 Mac 上可用：

```bash
mkdocs serve    # 本地预览，默认 http://127.0.0.1:8000
mkdocs build    # 输出到 site/
```

依赖：`pip install mkdocs mkdocs-material`。

## 改动本仓库时

- 改完 `mkdocs.yml` 先自检 YAML 缩进（`nav` 层级用 2 空格递进），云端无法用 `mkdocs build` 验证。
- 提交信息用中文或英文都行，说清楚"加了哪篇笔记 / 改了什么配置"即可。
- 不要把 Obsidian 库里的笔记正文复制进本仓库，除非明确要求。
